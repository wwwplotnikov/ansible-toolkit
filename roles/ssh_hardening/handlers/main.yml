---
- name: Restart sshd
  ansible.builtin.systemd_service:
    name: "{{ sshd_service_name }}"
    state: restarted
    daemon_reload: true

- name: Verify sshd config after restart
  ansible.builtin.command: sshd -t
  changed_when: false
  listen: Restart sshd
